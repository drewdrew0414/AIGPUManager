package com.drewdrew1.core.model;

/** Enumerates the built-in roles used by gpum authorization checks. */
public enum RbacRole {
    VIEWER,
    TENANT_ADMIN,
    OPERATOR,
    APPROVER,
    ADMIN
}
